# Drop square .webp profile images in this folder.
# Each file becomes a selectable profile picture automatically (the
# filename, minus extension, is used as its stored key). No code
# changes needed — Vite picks them up via import.meta.glob.
